[Seat:*]
# Show local desktop accounts in Slick Greeter instead of requiring manual
# username entry. The selected session is also written to ~/.dmrc below.
greeter-hide-users=false
greeter-show-manual-login=false
user-session=hyprland-uwsm
